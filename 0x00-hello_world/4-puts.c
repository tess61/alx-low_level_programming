#!/bin/bash
#include <stdio.h>
#include <string.h>
int main ( void ){
	    char str1[60];
	       strcpy(str1, "programming is like building a multilingual puzzle");
	          puts(str1);
		      return 0; }
