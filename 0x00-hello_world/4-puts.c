#include <stdio.h>
#include <string.h>
/**
* main-Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
	char str1[60];

	strcpy(str1, "programming is like building a multilingual puzzle\n");
	puts(str1);
	return (0);
}
