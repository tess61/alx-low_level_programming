#include <stdio.h>
/**
*main - print the last digit of the n valu3
*Return: always 0 (success)
*/
int main(void)
{
char ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
}
putchar('\n');
return (0);
}
