#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string followed by a new line..
 *
 *@str: the string to print.
 *
 */

void _puts(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
	printf("%d",(str[i]);
	}
	printf("\n");
}
